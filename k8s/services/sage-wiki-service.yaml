apiVersion: v1
kind: Service
metadata:
  name: sage-wiki
  labels:
    app.kubernetes.io/name: sage-wiki
    app.kubernetes.io/part-of: sage
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sage-wiki
  ports:
    - name: http
      port: 80
      targetPort: http
