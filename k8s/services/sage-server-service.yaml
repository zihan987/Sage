apiVersion: v1
kind: Service
metadata:
  name: sage-server
  labels:
    app.kubernetes.io/name: sage-server
    app.kubernetes.io/part-of: sage
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sage-server
  ports:
    - name: http
      port: 8080
      targetPort: http
