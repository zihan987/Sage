apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sage-rustfs-data
  labels:
    app.kubernetes.io/name: sage-rustfs
    app.kubernetes.io/part-of: sage
spec:
  accessModes:
    - ReadWriteOnce
  ${PVC_STORAGE_CLASS}
  resources:
    requests:
      storage: 50Gi
