apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sage-server-data
  labels:
    app.kubernetes.io/name: sage-server
    app.kubernetes.io/part-of: sage
spec:
  accessModes:
    - ReadWriteOnce
  ${PVC_STORAGE_CLASS}
  resources:
    requests:
      storage: 20Gi
