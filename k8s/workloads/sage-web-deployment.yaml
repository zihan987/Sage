apiVersion: apps/v1
kind: Deployment
metadata:
  name: sage-web
  labels:
    app.kubernetes.io/name: sage-web
    app.kubernetes.io/part-of: sage
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: sage-web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: sage-web
        app.kubernetes.io/part-of: sage
    spec:
      containers:
        - name: sage-web
          image: "${SAGE_WEB_IMAGE}"
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
          env:
            - name: TZ
              value: Asia/Shanghai
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /sage/
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /sage/
              port: http
            initialDelaySeconds: 20
            periodSeconds: 30
